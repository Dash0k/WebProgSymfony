{% extends 'base.html.twig' %}

{% block title %}Admin panel!{% endblock %}

{% block body %}

<div class="example-wrapper">
    <p>
    <a href="{{ path('app_article_new') }}" class="btn btn-default"><button class="btn btn-primary">{{ 'Create title'|trans }}</button></a>
    </p>
    <p>
    <a href="{{ path('homepage') }}" class="btn btn-default"><button class="btn btn-primary">{{ 'Home'|trans }}</button></a>
    </p>
</div>
{% endblock %}
